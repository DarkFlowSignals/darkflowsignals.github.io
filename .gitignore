# v5.10 split (2026-04-20):
# signals_premium.json IS now deployed (strike/expiry/entry/current/peak
# premium, gated by weekly-rotating access_key URL param). The URL key check
# lives in dashboard.html and premium-only fields show only when URL key
# matches the access_key in this file.
# signals_internal.json stays local-only (adds broker mechanics like
# alpaca_status/decision/fill_price/entry_time that must never ship).
signals_internal.json

# Operational / audit artifacts - never deploy.
task_events.jsonl
signal_decisions.csv
alpaca_trade_log.csv
hw_tracking.json
pending_entries.json
trade_log.csv
_smoke_*.py

# Local editor / IDE noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
_mockup*.html
